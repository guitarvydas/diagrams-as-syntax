1. load md2swipl.html into a browser
2. copy ../run.md into the textarea "code markdown source"
3. scroll to bottom and press "generate"
4. copy code (text) from textarea "transpiled to bash" into ../run.bash
